# react-runner@1 declares a stale React peer (^16||^17||^18) but works with React 19
# (proven in bosphorify-web). It's an OPTIONAL peer here, so only /runner consumers
# ever install it. Relax strict peer resolution for this repo's own install/CI.
legacy-peer-deps=true
